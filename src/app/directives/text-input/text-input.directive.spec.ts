import { TextInputDirective } from './text-input.directive';

describe('TextInputDirective', () => {
  it('should create an instance', () => {
    const directive = TextInputDirective;
    expect(directive).toBeTruthy();
  });
});
