apiVersion: v1
kind: Service
metadata:
  name: {{ include "insighttellers-web.fullname" . }}
  labels:
    {{- include "insighttellers-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insighttellers-web.selectorLabels" . | nindent 4 }}
